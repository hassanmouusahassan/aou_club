import 'package:flutter/material.dart';

class ClubInfo extends StatelessWidget {
  const ClubInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("hey omar here is the clubs Info page")),
    );
  }
}
