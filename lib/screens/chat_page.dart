import 'package:flutter/material.dart';

class ChatPage extends StatelessWidget {
  const Chatpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("hey omar here is the chat page")),
    );
  }
}

